export function SkeletonInput() {
  return (
    <div
      style={{
        height: 40,
        width: "100%",
        background: "#e5e7eb",
        borderRadius: 6,
        marginBottom: 16,
        animation: "pulse 1.5s ease-in-out infinite"
      }}
    />
  );
}
